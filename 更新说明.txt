V3 详情页自然画幅补丁（已包含此前“可读性 + 档案不裁切”调整）

解决的问题：
1. 竖幅照片右侧不再出现大块灰色空槽。
2. 图片按真实比例显示，最大高度约为浏览器高度的 84%。
3. EXIF 信息紧贴图片右侧，而不是固定在页面最右端。
4. 保留轻微错落排版，但错落通过位置与暖白留白实现，不再使用灰色填充区域。
5. 手机端自动回到单栏：完整图片在上，EXIF 在下。
6. 同时保留此前的小字可读性增强与档案页不裁切规则。

GitHub 只需替换：
assets/css/site.css

然后 Commit changes，等待 Pages 发布后强制刷新浏览器。
